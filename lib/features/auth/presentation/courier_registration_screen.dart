import 'package:flutter/material.dart';

class CourierRegistrationScreen extends StatelessWidget {
  const CourierRegistrationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
          child: Text(
              'Courier registration form placeholder - API wired in Auth/Register flow')),
    );
  }
}
