name: bantucart

description: Bantucart cross-platform Flutter app.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  firebase_core: ^3.6.0
  firebase_analytics: ^11.3.3
  firebase_crashlytics: ^4.1.3
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # Networking + secure storage (Phase 2 ready)
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  drift: ^2.22.1
  path: ^1.9.0
  path_provider: ^2.1.5
  sqlite3_flutter_libs: ^0.5.26
  permission_handler: ^11.3.1
  firebase_messaging: ^15.1.5
  flutter_local_notifications: ^17.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  mocktail: ^1.0.4
  drift_dev: ^2.22.1

flutter:
  uses-material-design: true

